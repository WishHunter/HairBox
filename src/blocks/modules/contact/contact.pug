mixin contact()
  section.contact
    .contact__hero
    .container
      h2.contact__title Свяжитесь с нами
      .contact__flex
        .contact__form-block
          form.contact__form(action="")
            input.contact__input.contact__input--small(type="text")
            input.contact__input.contact__input--small(type="text")
            input.contact__input(type="text")
            textarea.contact__textarea(name="")
            button.main-btn.contact__button
        .contact__info-block
          .contact__info
